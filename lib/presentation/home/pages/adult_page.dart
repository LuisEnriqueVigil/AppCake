import 'package:flutter/material.dart';

class AdultCakePage extends StatelessWidget {
  const AdultCakePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child:Text('Pagina de pasteles adultos')
      ),
    );
  }
}